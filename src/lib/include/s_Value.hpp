#pragma once

/**
 * Value
 */
struct s_Value {

    /**
     * Pointer pointing to the address of value
     */
    char *address;

    /**
     * Length of value from address (char pointer)
     */
    int length;
};
